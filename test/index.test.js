const { describe, it } = require('mocha')
const { expect } = require('chai')

const clone = require('ramda/src/clone')

const s = require('../index')

describe('package exports existence', () => {
  it('is tbd', () => {})
})
